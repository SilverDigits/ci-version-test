
mode: ContinuousDelivery

next-version: 4.1.0

branches:
  main:
    regex: ^main$
    label: rc
    increment: Inherit
    is-main-branch: true

  default:
    regex: .*
    label: beta
    increment: Inherit